# kubernetes-media-manifests

These are manifests I use for my internal media infrastructure deployed on kubernetes. They use a backend nfs server for persistent storage.
To deploy:

kubectl create -f YAML_FILE
